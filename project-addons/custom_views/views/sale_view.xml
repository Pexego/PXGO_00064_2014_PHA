<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
        <!-- Modifications to hide Sale Confirm Button to comercials -->
        <record model="ir.ui.view" id="view_order_form">
            <field name="name">sale.order.form</field>
            <field name="model">sale.order</field>
            <field name="priority">16</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <field name="partner_id" position="replace">
                    <field name="partner_id"
                           on_change="onchange_partner_id(partner_id, context)"
                           domain="[('customer','=',True)]"
                           class="oe_edit_only"
                           context="{'concatenate_name_commercial': 1, 'search_default_customer': 1, 'show_address': 1}"
                           options='{"always_reload": True}'/>
                    <field name="partner_id"
                           on_change="onchange_partner_id(partner_id, context)"
                           domain="[('customer','=',True)]"
                           class="oe_read_only"
                           context="{'search_default_customer': 1, 'show_address': 1}"
                           options='{"always_reload": True}'/>
                </field>
                <field name="partner_invoice_id" position="replace">
                    <field name="partner_invoice_id"
                           groups="sale.group_delivery_invoice_address"
                           context="{'concatenate_name_commercial': 1, 'default_type': 'invoice'}"/>
                </field>
                <field name="client_order_ref" position="replace">
                    <field name="client_order_ref" string="Order reference" required="True"/>
                </field>
            </field>
        </record>
    </data>
</openerp>
